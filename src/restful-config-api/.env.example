# Example environment configuration for RESTful Config API Sample
# Copy this file to .env and update with your actual values

# The URL of the Identity Provider of the System
SYSTEM_IDENTITY_PROVIDER=https://your-management-server.example.com

# Whether the XProtect server uses TLS (true/false)
LEGACY_USE_TLS=true

# The hostname of the Management Server in XProtect
LEGACY_MANAGEMENT_SERVER=your-management-server.example.com

# Note: This sample is designed to run in a container where these
# environment variables are provided by the container runtime.
# For local development, create a .env file with your actual values.